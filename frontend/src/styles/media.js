import { sizes } from "src/styles/breakpoints"

export const devices = {
  bigScreen: `(max-width: ${sizes.bigScreen})`,
  mediumScreen: `(max-width: ${sizes.mediumScreen})`,
  horizontalTablet: `(max-width: ${sizes.horizontalTablet})`,
  verticalTablet: `(max-width: ${sizes.verticalTablet})`,
  smartphone: `(max-width: ${sizes.smartphone})`,
}
